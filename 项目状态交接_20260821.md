# Crestron 音频矩阵项目 — 会话交接总结

> 日期：2026-08-21 14:19
> 用途：新窗口继续项目前先读本文件，了解当前状态与未解决问题

---

## 一、项目位置（重要）

| 用途 | 路径 |
|------|------|
| **B = 代码工作地** | `D:\Crestron\simpl#`（git 根），项目在 `4-Series\CrestronDeviceLibrary\` |
| **A = 仅备份** | `D:\Work\prj\simpl#\CrestronDeviceLibrary-repo`（不要再改） |
| **3 系工程（VS2008）** | `D:\Crestron\simpl#\3-Series\CrestronDeviceLibrary\`（链接引用 4-Series 的同一份 .cs） |
| **4 系工程（VS2026）** | `D:\Crestron\simpl#\4-Series\CrestronDeviceLibrary\` |
| **★ 用户 SIMPL 工程（编译地）** | `C:\Users\YSL\Desktop\cp4\`（Demo.smw + 各 .usp + clz） |
| **Skill 权威源** | `D:\Crestron\skill\Crestron_SIMPL+宏与SIMPLSharp库_合并技能\SKILL.md`（改后 cp 到用户级 `~/.workbuddy/skills/crestron-simpl-simplsharp/SKILL.md`） |

**★ 关键教训（今天踩坑）**：
- 改仓库 `Samples\*.usp` 后 **必须同步到桌面 cp4**（`cp` 覆盖），用户编译用的是桌面那份！
- 3 系 clz 必须在 **VS2008 里重新生成**才更新（obj 缓存坑）
- `.clz` 是 ZIP 包，内含 dll；.NET 字符串是 UTF-16LE，grep 搜不到要用 `s.encode('utf-16-le')`

---

## 二、设备信息

| 设备 | IP:端口 | 说明 |
|------|---------|------|
| MC3 中控 | 192.168.0.100:22(SSH) / 41795(console) | 3 系测试主机 |
| StageCraft 矩阵 | 192.168.0.222:1698 | 二进制+ASCII 协议 |
| Biamp 主用 | 192.168.0.24:23 (Telnet) | 16 路 Level1 旧固件 |
| Biamp 备用 | 192.168.0.25:23 (Telnet) | 6 路新固件 |
| StageCraft 直连测试 | 登录包 82 7d 01 01 01 05 00 61 64 6d 69 6e 7d 82，无应答（疑单连接被 MC3 占） | |

---

## 三、已完成的功能/修复（今天）

### 1. 页面驱动轮询（用户提议）
- 只有切到对应子页才读码：输入页(1)/输出页(2)/混音页(3)，离开 SetPollMode(0)
- StageCraftMatrix.cs：`SetPollMode` + PollTick 按模式刷新（400ms）
- RedundantAudioMatrix.cs：接口+实现加 `SetPollMode`（Mirror 透传）
- Biamp：空实现（订阅模式无需轮询）

### 2. 初始化推送默认状态（解决"传程序后界面空白"）
- StageCraft：`PushDefaultStates`（OnConnectComplete 后推电平0dB/静音off/音量表0 + 分批读回）
- Biamp：`PushDefaultStates`（MarkReady 订阅就绪后推）
- SonyViscaCamera：新增 `PushDefaultStates`（摄像机1/速度/保存模式off），usp Main 调用

### 3. mute 行为统一（推子不动、电平熄灭、音量可调）
- `UpdateLevelFb`：推子始终显示实际 dB（删掉 Biamp 的 mute 归零+"Mute"文本分支）
- `SetInputMute/SetOutputMute`：mute 时**立即推 0 到音量表**（不等轮询/订阅，解决"偶尔不熄灭"时序问题）
- 用户明确：**不要**推子归零，只要音量表熄灭

### 4. meter 去掉 dirty check（解决"信号停了电平条卡住"）
- **教训**：dirty check 只用于状态类（mute/路由/电平档位），**不用于实时流（VU meter）**——"不变"本身是有效信息（信号停了）
- StageCraft.UpdateMeter + Biamp.ParseMeterArray 的 meter 分支无条件推

### 5. 冗余主从联动（主机动从机跟随）
- `SyncToPeer`/`SyncToPeerAnalog`：设备推送时同步到对端（mute/路由/电平）
- `_syncingToPeer` 防循环标记
- **路由不同步**（路由是状态，设备推送只更新显示，避免 A→B→A 循环）

### 6. 切页保持选中输出（_selOutN 追踪）
- **★ SIMPL+ 里 DIGITAL_OUTPUT 不可读**！之前写的 `IF (MatrxOUTFb[n])` 永远不成立
- 修复：`PUSH Matrx_OUT` 里记录 `_selOutN` 全局变量，`CHANGE mixpage_fb` 里按 `_selOutN` 同步 `SelectOutput`

### 7. SIMPL+ 语法修正（编译报错 1304/1002/1200/1400）
- FOR 用 `FOR (n = 1 TO CH)`（不是 C 风格）
- 比较用 `=`（`==` 非法）
- 变量声明必须在块顶
- 无 BREAK（用 `IF (found = 0)` 包裹）
- 已同步到 skill（5b/5c 节）

---

## 四、当前未解决问题（★ 重点）

### ★★ 问题A：VTP 显示两台 Biamp 都不在线，但实际都在线
- **状态**：未解决，正在诊断
- **已做**：给 RaiseStatus 加诊断日志（`[Redundant] Status online=[True,True] leader=0 (fb=set/set)`）；Resync 延迟 2s（避免订阅建立前发 288 条命令风暴）
- **4 系 dll 已确认含最新代码**（Status online=/schedule Resync in 2s）
- **★ 3 系 obj dll 还是旧的**（无最新代码）——用户**还没在 VS2008 重新生成**
- **下一步**：
  1. VS2008 重新生成 3 系工程 → 拷 clz 到桌面 cp4
  2. SIMPL Windows 重新编译上传
  3. Toolbox Console 看日志：
     - `[Redundant] device idx=0 online, schedule Resync in 2s` → 连接事件触发
     - `[Redundant] Status online=[True,True] leader=0 (fb=set/set)` → 重点！fb=set/null 判断 .usp 注册
     - `[Redundant] Resync idx=0` → 延迟重同步执行
  4. 如果 `online=[True,True]` 但 VTP 显示不在线 → .usp join 问题
  5. 如果 `online=[False,False]` → 设备连接/订阅未建立

### 问题B：之前反馈"所有控制功能失效"
- 疑似与问题A同源（3 系 clz 旧）或 VTP join 错位
- 需 Console 日志确认

### 问题C：切页高亮同步（_selOutN 方案）
- 已改 4 个 .usp，待用户编译验证

---

## 五、3 系性能要点（用户要求 3/4 系都适配且不卡）

- 轮询周期 400ms（3 系友好）
- meter 无 dirty check（实时）
- 回调峰值 ~120/秒（接近 3 系上限，dirty check 降低实际推送）
- **Resync 延迟 2s** 避免命令风暴（288 条命令）
- VerboseLog 已开（诊断用，StageCraft 的；Biamp 的 VerboseLog 需检查）——**测完记得关**（`private static readonly bool VerboseLog = true` → false）

---

## 六、测试流程（每次改完）

1. 改仓库 `4-Series\CrestronDeviceLibrary\` 下源码
2. **VS2026 编译 4 系**验证（dotnet build，0 错误）
3. **VS2008 重新生成 3 系工程**（出 3 系 clz）
4. **拷 clz + usp 到桌面 cp4**（clz：`3-Series\...\bin\Debug\CrestronDeviceLibrary.clz`；usp：`4-Series\...\Samples\*.usp`）
5. SIMPL Windows 重新编译 → 上传 MC3
6. Toolbox Console 看日志验证

---

## 七、关键文件清单

| 文件 | 作用 |
|------|------|
| `Devices\StageCraftMatrix.cs` | StageCraft 逻辑（轮询/初始化/mute） |
| `Devices\BiampTesiraMatrix.cs` | Biamp 逻辑（订阅/初始化/mute） |
| `Devices\RedundantAudioMatrix.cs` | 双机热备（SyncToPeer/防循环/Resync） |
| `Devices\SonyViscaCamera.cs` | VISCA 摄像头 |
| `Samples\AudioMatrix_StageCraft.usp` | StageCraft 单台薄壳 |
| `Samples\Redundant_AudioMatrix_StageCraft.usp` | StageCraft 冗余薄壳 |
| `Samples\Biamp_Tesira.usp` | Biamp 单台薄壳 |
| `Samples\Redundant_Biamp_Tesira.usp` | **★ Biamp 冗余薄壳（用户当前在测）** |
| `Samples\CAM_SONY_VISCA.usp` | VISCA 薄壳 |

---

## 八、Skill 维护（用户约定）

- 权威源 `D:\Crestron\skill\Crestron_SIMPL+宏与SIMPLSharp库_合并技能\SKILL.md`
- 改完 **cp 到用户级** `~/.workbuddy/skills/crestron-simpl-simplsharp/SKILL.md`
- 今天已补充：SIMPL+ 语法（FOR TO/= 比较/无 BREAK/变量块顶）+ DIGITAL_OUTPUT 不可读 + PUSH 副作用追踪
